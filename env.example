# Local: copy this file to secrets.env and fill in values (secrets.env is gitignored).
# If your virtualenv folder is NOT named ".env", you may use a ".env" file instead.
# On Railway/Render/etc., set variables in the host dashboard (no file needed).

GROQ_API_KEY=

FLASK_DEBUG=0
PORT=5000
